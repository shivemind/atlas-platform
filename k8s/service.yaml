apiVersion: v1
kind: Service
metadata:
  name: atlas-platform
  namespace: atlas-production
  labels:
    app: atlas-platform
spec:
  type: ClusterIP
  selector:
    app: atlas-platform
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
